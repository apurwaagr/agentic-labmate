"""
Backend package namespace for parser + literature QC + API server modules.

Current layout keeps legacy root modules for compatibility while exposing
stable package imports under `backend.*`.
"""

